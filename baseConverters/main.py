import sys

# Use
#   alias factorTree='cd ~ && cd rootdir-scripts && cd factorTree && f(){pipenv run python3 factorTree.py "$1"; unset -f f; cd ~;}; f'
# to run from terminal


if __name__ == "__main__":
    # argv[1] will be number
    # argv[2] will be base
    # argv[3] will be convert to

    # num = int(sys.argv[1])
    # args = sys.argv

    # if len(args) < 3:
    #     print("Need at least 2 operators")
    # else:
    #     numArgs = args[1:]
    #     sum = 0
    #     for arg in numArgs:
    #         temp = float(arg)

    print("x")

    print("y")
